AMAZON_SCRAPPER_SVC_HTTP_PORT?=8080

init:
	go mod download

build:
	go build -v -x -o bin/main cmd/server/*.go

run:
	AMAZON_SCRAPPER_SVC_HTTP_PORT=8080

docker:
	docker build -f Dockerfile -t 4m3ndy/amazon-scrapper-service:v1.0.0
